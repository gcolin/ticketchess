# Configuration for Playwright integration tests (no secrets — safe to commit)
mail.USER_NAME=test@test.com

admins=test@test.com
membership.notif.emails=admin@test.com

title=Ticket Chess Test
org.name=Ticket Chess Test
org.email=test@test.com
org.address=1 rue Test, 00000 Ville, France
baseurl=http://localhost:8080
contact.url=http://localhost:8080/contact
source.url=https://github.com/gcolin/ticketchess

stripe.simuled=true
stripe.card.memberships=true
stripe.card.events=true

testmode=true

auth.USER_EMAIL=test@test.com
auth.USER_NAME=Test
jwt.key=integration-test-jwt-key-not-for-production-use-only

db.h2.loadPostgresDump=false
pendingQueueOffset=0
